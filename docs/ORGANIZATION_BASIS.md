# Machine Organization, Consolidation & Backup Basis

Dataset-driven basis for organizing, deduplicating, and backing up the
machine. The authoritative dataset is `reports/machine_inventory.json`
(generated by `tools/machine_inventory.py`; digest in
`reports/machine_inventory.md`). Everything below is keyed to that dataset so
each action is reviewable before execution.

## Scope honesty

This scan ran in the **remote session container**. It fully covers the
repository and the container toolchain; it does **not** see the operator's
Linux Mint machine (`/home/jmmint`) or `/root/embroidery_business_agent_system`.
To produce the same dataset there, run on that machine:

```bash
EMBIZ_SCAN_ROOTS=/home/jmmint:/root python3 tools/machine_inventory.py
```

## What the dataset showed here (2026-07-16 scan)

- 27,781 files across `/home/user`, `/root`, `/opt`; 8.1 GB of 252 GB used.
- 4 git repositories: this repo (the only project repo) + container
  toolchain clones (`/opt/rbenv`, `/opt/rbenv/plugins/ruby-build`,
  `/opt/nvm`) — system-managed, not project data, **do not touch**.
- 5,728 duplicate groups ≈ 354 MB — ~93% is container toolchain
  (node20/21/22 openssl headers, rbenv rubies). Not project debris; leave
  to the image maintainers.
- 246 repo-internal duplicate groups ≈ 26 MB, almost all deliberate:
  per-pass E2E evidence copies, per-job artwork copies (gitignored), and
  the `vector_source/` ↔ `vectorized_svg/` canonical pairs. Identical
  stitch previews across passes are **determinism evidence**, not waste.
- 533 embroidery machine files (202 EXP / 207 PES / 124 INF) + previews.
- 42 sensitive-file locations (`/root/.aws`, `.ssh`, `.env`-style names) —
  paths recorded in the dataset, contents never read.
- 0 debris files (`.pyc`, `.DS_Store`, editor backups) in scanned roots.

## Repository layout (the organized target — already in effect)

```
docs/requirements/   canonical BRD, matrix, GAP_ANALYSIS, SPEC_COVERAGE
agents/contracts/    compiled 18×M runtime contracts (generated artifacts)
skills/              SKILL.md-spec agent skills + shared references
local_agents/        runtime: intake, jobs, agent_bus, dashboard,
                     webhook_api, personas/contracts, daemons, systemd units
knowledge/library/   retrieval corpora (per-corpus knowledge_objects.jsonl)
input_images/        customer/source rasters — NEVER committed (policy)
vector_source/       canonical SVGs        vectorized_svg/  vectorizer output
stitch_plans/        production stitch outputs (EXP/PES/INF/BMP/preview/JSON)
production_runs/     ruler-review JPEG triplets per finalized image
jobs/                canonical job records + audit (artwork copies ignored)
reports/             evidence: e2e passes, inventory, audits, ledgers, alerts
scripts/ tools/      compilers, validators, regression, backup, inventory
```

Cleanup applied in this pass: `reports/e2e/pass_*/​*_work.svg` (raster-
embedding working copies, ~8.7 MB each, regenerable from the production SVG +
input image) are no longer committed — the reviewed artifact is the cleaned
`*_production.svg`. Consolidation candidate flagged, not executed: 9
byte-identical `3_stitchplan_on_ruler.jpg` files under `production_runs/`
predate this branch; rewriting them belongs in its own change.

## Backup procedure (implemented, executed)

`scripts/backup_machine.py` produces per-run under `/home/user/backups/<ts>/`:
`repo.bundle` (full git history, bundle-verified), `worktree.tar.gz`
(complete working tree incl. gitignored runtime evidence), `MANIFEST.json`
(sha256 for every file and both archives). Executed 2026-07-16:
105.6 MB bundle + 97.8 MB tarball, both checksum-verified. The pushed GitHub
remote is the continuous offsite copy of tracked content.

**AWS stage — cannot run from the session container; handed off as a script.**
This environment's `AWS_*` variables are egress-proxy plumbing (STS rejects
them with `InvalidClientTokenId`), and the uploaded credential reports are
redacted (masked key IDs/fingerprints only, no secret values). The AWS backup
must therefore run **on the operator's machine**, where the real credentials
and the real files both live. One command does inventory + local backup +
S3 upload:

```bash
scripts/bootstrap_offsite_backup.sh s3://<your-bucket>/embiz
```

It reads credentials from the standard AWS chain (never the command line),
STS-verifies identity first, and runs `backup_machine.py --upload` (uploads
bundle, tarball, and manifest with sha256 object metadata).

### Security finding from the credential search (act before backing up)

The operator's own AWS-credential search report flags access key
`AKIA…CHVX` as **Active** and present **in plaintext** in `Downloads/
rootkey.csv` beside its secret, and copied into multiple documents,
backups, and `project_credentials_aggregate.txt`. `rootkey.csv` is the file
AWS emits when you create an access key for the **account root**. Using a
widely-copied root key for routine backups is a standing risk. Recommended:

1. Create a scoped IAM user (`embiz-backup`) with an S3-only policy limited
   to the backup bucket (template printed by the bootstrap script).
2. Configure a named AWS profile for that user.
3. **Delete the root access key** in the IAM console and purge the plaintext
   copies (`rootkey.csv`, the aggregate files) found in the report.

`bootstrap_offsite_backup.sh` enforces this: it **refuses to upload** when
the resolved identity is the account root unless `--allow-root` is passed.

## Standing rules

1. Original customer artwork is never committed (`input_images/` ignored).
2. Generated artifacts (contracts, dashboards) are rebuilt, never hand-edited.
3. Runtime state (`local_agents/state/`) is durable on the host, never
   committed.
4. Every dedupe/reorganization action must cite its dataset row
   (`machine_inventory.json` duplicate group) before deletion.
5. Secrets live only in environment files (mode 600); the inventory records
   their locations, never their contents.
